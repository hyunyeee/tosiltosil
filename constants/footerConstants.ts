export const FOOTER_CONFIG = {
  policy: ["/entry", "/landing", "/login"],
  nav: [
    "/home",
    "/alarm",
    "/profile/edit",
    "/category/edit",
    "/friend",
    "/calendar",
  ],
} as const;
